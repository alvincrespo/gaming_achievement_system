<div class="px-4 sm:px-6 lg:px-8">
  <div class="sm:flex sm:items-center">
    <div class="sm:flex-auto">
      <h1 class="text-base font-semibold text-gray-900">Gaming Guilds</h1>
      <p class="mt-2 text-sm text-gray-700">This demo shows how query performance varies dramatically based on guild size. Notice how "Mega" guilds with 10k+ achievement unlocks require window functions for acceptable performance.</p>
    </div>
  </div>
  <div class="mt-8 flow-root">
    <div class="-mx-4 -my-2 overflow-x-auto sm:-mx-6 lg:-mx-8">
      <div class="inline-block min-w-full py-2 align-middle sm:px-6 lg:px-8">
        <div class="overflow-hidden shadow-sm ring-1 ring-black/5 sm:rounded-lg">
          <table class="min-w-full divide-y divide-gray-300">
            <thead class="bg-gray-50">
              <tr>
                <th scope="col" class="py-3.5 pr-3 pl-4 text-left text-sm font-semibold text-gray-900 sm:pl-6">Guild Name</th>
                <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">Unlocks</th>
                <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">Size Category</th>
                <th scope="col" class="relative py-3.5 pr-4 pl-3 sm:pr-6">
                  <span class="sr-only">Actions</span>
                </th>
              </tr>
            </thead>
            <tbody class="divide-y divide-gray-200 bg-white">
              <% @guilds.each do |guild| %>
                <tr>
                  <td class="py-4 pr-3 pl-4 text-sm font-medium whitespace-nowrap text-gray-900 sm:pl-6">
                    <%= link_to guild.name, guild_path(guild), class: "text-indigo-600 hover:text-indigo-900" %>
                  </td>
                  <td class="px-3 py-4 text-sm whitespace-nowrap text-gray-500"><%= number_with_delimiter(guild.unlock_count) %></td>
                  <td class="px-3 py-4 text-sm whitespace-nowrap text-gray-500">
                    <span class="badge <%= guild_size_class(guild.unlock_count) %>">
                      <%= guild_size_label(guild.unlock_count) %>
                    </span>
                  </td>
                  <td class="relative py-4 pr-4 pl-3 text-right text-sm font-medium whitespace-nowrap sm:pr-6">
                    <%= link_to "View", guild_path(guild), class: "text-indigo-600 hover:text-indigo-900 mr-2" %>
                    <%= link_to "Compare Queries", compare_queries_guild_path(guild), class: "text-indigo-600 hover:text-indigo-900" %>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>
